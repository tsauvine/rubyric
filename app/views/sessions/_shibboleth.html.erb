<div class="well">
  <p class="hint"><%=t :sessions_shibboleth_hint %></p>

  <%= link_to "/Shibboleth.sso/wayf?target=" + url_for(:action => 'shibboleth', :controller => 'sessions', :only_path => false, :protocol => 'https') do
    image_tag('haka_portrait_large.gif', {:alt => '[Haka Login]'})
  end %>
</div>
