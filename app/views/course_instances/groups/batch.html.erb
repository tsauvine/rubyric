<!-- exercises/batch_assign.html.erb -->

<h1>Create groups</h1>

<%= form_tag batch_course_instance_groups_path(@course_instance), :html => { :multipart => true } do |f| -%>
  <div class="hint">
    Paste the student list here.
    <ul>
      <li>Each row represents one group.</li>
      <li>Separate students by comma.</li>
      <li>Students can be identified with either studentnumber or email.</li>
      <li>Duplicate groups are not created, ie. you can safely upload the same list multiple times.</li>
    </ul>
  </div>
  <%= text_area_tag :paste, nil, :rows => 25, :class => 'span12' %>
  <p><%= submit_tag 'Upload', :class => 'btn btn-primary' %></p>
<% end -%>

<p><%= link_to 'Back', course_instance_groups_path(@course_instance) %></p>
