<%= @course.code %> <%= @course.name %>

<%=t '.you_have_been_added' %>
<% @group.users.each do |user| %>  <%= user.name %>
<% end %><% @group.group_invitations.each do |invitation| %>  <%= invitation.email %>
<% end %>

<%=t '.please_confirm' %>

<%= join_group_url(:id => @group.id, :token => @invitation.token, :protocol => 'https://') %>
