== Install Ruby environment
# Install ruby and rubygems

# Install bundler using rubygems
sudo gem install bundler


== Install Rubyric

cd rubyric

# Install gems
bundle install

# Configure
cp config/initializers/secret_token.rb.base config/initializers/secret_token.rb
cp config/initializers/settings.base config/initializers/settings.rb
cp config/database.yml.base config/database.yml

# Create database, and put password and username to config/database.yml

# Initialize database
rake db:setup


== Start server

rails server

# or use mongrel which is faster:
rails server mongrel
