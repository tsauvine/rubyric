== Install Ruby and Rails

aptitude install ruby1.8 ruby1.8-dev rubygems
gem install rake
gem install -v=2.3.9 rails
gem install -v=2.0.4 delayed_job
gem install pg    # or some other database adapter


== Clone the git repository
git clone git://github.com/tsauvine/rubyric.git


== Install Rubyric

cd rubyric

# Configure settings
cp config/initializers/settings.rb.base config/initializers/settings.rb
cp config/database.yml.base config/database.yml

# Create a database, and put password and username to config/database.yml. The database user must have the privileges to create databases for the next step to work.

# Initialize database (admin/admin user will be created. Remember to change the password.)
rake db:setup


== Start server

# For testing or development:
script/server

# For production, we currently use Phusion Passenger (http://www.modrails.com/)

# Start the background worker that sends the emails
script/delayed_job start
